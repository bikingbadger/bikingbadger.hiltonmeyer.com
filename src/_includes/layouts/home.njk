<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>{{ renderData.title or title or metadata.title }}</title>
    <meta
      name="Description"
      content="{{ renderData.description or description or metadata.description }}"/>
    <link rel="stylesheet" href="/assets/css/tailwind.css"/>    
  </head>

  <body class="bg-gray-100 text-gray-900 leading-normal text-base tracking-normal">

{# style="grid-template-columns:1fr auto 1fr;" #}
    <header class="h-screen bg-teal-500 grid" >
      {# <input type="checkbox" id="nav-toggle" class="hidden">
      <label for="nav-toggle" class="inline-block self-center ml-1 lg:hidden" >
        <img src="/assets/img/hamburger-menu.svg" alt="">
      </label> #}
      <div class="grid justify-center h-screen"><h1 class="text-teal-100 font-black text-4xl md:text-5xl uppercase self-center">{{ metadata.title }}</h1></div>
      <div class="bg-teal-300  left-menu -mt-6 grid justify-center grid-flow-col">
        {%- for nav in collections.nav -%}
          <a class="text-teal-800 hover:text-white no-underline uppercase mx-2 " href="{{ nav.url | url }}">{{ nav.data.navtitle }}</a>
        {%- endfor -%}
      </div>
    </header>

    <main class="container mx-auto ">
      {{ content | safe }}
    </main>

    <footer></footer>
  </body>
</html>
